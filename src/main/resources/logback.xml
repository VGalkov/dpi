
<configuration scan="true" scanPeriod="10 seconds">

    <!-- Аппендер для файла (ротация по дням) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/app.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="ru.galkov.servers.ProxyHandler" level="WARN"/>
    <logger name="ru.galkov.servers.IptablesIpBlocker" level="INFO"/>
    <logger name="ru.galkov.AppConfig" level="INFO"/>
    <logger name="ru.galkov.util.ProxyHandlerHelper" level="INFO"/>
    <logger name="org.xbill.DNS.SimpleResolver" level="DEBUG"/>

    <logger name="ru.galkov.servers.DnsServer" level="INFO"/>

    <root level="DEBUG">
        <appender-ref ref="FILE"/>
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>